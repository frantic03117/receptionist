// import React from "react";
import { CircularProgress, Box } from "@mui/material";

const Loader = () => {
    return (
        <Box className="h-lvh flex justify-center items-center">
            <CircularProgress />
        </Box>
    );
};

export default Loader;
